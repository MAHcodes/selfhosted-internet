services:
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    ports:
      - 4533:4533
    restart: unless-stopped
    volumes:
      - data:/data
      - /home/ali/Music/:/music:ro

volumes:
  data:
